[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"
[project]
name = "cnbe32"
version = "0.2.0"
description = "CNBE-32: Chinese Native Binary Encoding"
readme = "README.md"
license = {text = "Mulan PSL v2"}
requires-python = ">=3.10"
dependencies = ["numpy>=2.0"]
